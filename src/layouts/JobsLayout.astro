---
import type { CollectionEntry } from 'astro:content'
import { getCollection } from 'astro:content'
import { Block, Blocks, JobDeck, MainLayout } from 'fulldev-blocks'
import { Prose, Section } from 'fulldev-ui'

type Props = CollectionEntry<'pages'>

const { data, slug, ...rest } = Astro.props

const childPages = (
  await getCollection('pages', (page) => page.slug.startsWith(slug))
).map((page) => ({ href: `/${page.slug}/`, ...page.data }))
---

<MainLayout
  class:list={'page'}
  data={data}
  {...rest}
>
  <Block
    _bookshop_name="Hero"
    heading={data.title}
    text={data.description}
    {...data}
    {...data.hero}
  />
  <JobDeck cards={childPages} />
  <Section
    size="lg"
    style={{ '--screen': '768px' }}
  >
    <Prose id="prose">
      <slot />
    </Prose>
  </Section>
  <Blocks blocks={data.blocks} />
  <slot />
</MainLayout>
