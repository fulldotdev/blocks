---
import { getEntry } from 'astro:content'
import { generateRadixColors } from '../colors/generateRadixColors'
import Hero from '../components/blocks/Hero.astro'

const { data } = await getEntry('pages', 'index')

const colors = generateRadixColors({
  appearance: 'light',
  accent: '#000',
  gray: '#000',
  background: '#fff',
})
console.log(colors)
---

<Hero />
