---
import type { HTMLTag } from 'astro/types'
import { getEntries } from 'astro:content'
import {
  Button,
  Card,
  Column,
  Grid,
  Heading,
  Image,
  Section,
  Spread,
  Text,
  Wrap,
} from 'fulldev-ui'
import type { Block } from '../../schemas/block'

type Props<As extends HTMLTag = 'section'> = Block<As>

const { heading, text, pages, buttons, ...rest } = Astro.props

const entries = await getEntries(pages)
---

<Section {...rest}>
  <Spread>
    <Column>
      <Heading html={heading} />
      <Text html={text} />
    </Column>
    <Wrap>
      {buttons?.map((button, i) => <Button {...button} />)}
    </Wrap>
  </Spread>
  <Grid>
    {
      entries?.map(({ slug, data: { heading, image, text } }) => (
        <Card
          href={`/${slug}/`}
          frame="panel"
        >
          <Image
            src={image}
            position="inset"
          />
          <Heading
            html={heading}
            level={3}
          />
          <Text html={text} />
        </Card>
      ))
    }
  </Grid>
</Section>
